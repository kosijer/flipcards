/**
 * UI and behaviour constants used across the app.
 */

/** App display name (header and document title). */
export const APP_TITLE = 'Biramo kaznu'

/** Delay in ms before the close button is shown on the fullscreen card view. */
export const FULLSCREEN_CLOSE_BUTTON_DELAY_MS = 5000

/** Message shown while card images are loading. */
export const LOADING_CARDS_MESSAGE = 'Loading cards…'
